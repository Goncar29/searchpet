{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/share/:token", "destination": "/api/share/:token" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
